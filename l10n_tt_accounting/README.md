# Trinidad & Tobago Accounting Localization

Safe compliance profile build v1.6.1.

Fixes:
- Adds missing `_ensure_compliance_profile()` helper used by the setup apply button.
- Keeps compliance in a separate model, not directly on `res.company`.

Menu:
Accounting → Configuration → Trinidad & Tobago → Compliance Profile
